<?php

namespace {COMPANYNAME}\{MODULENAME}\Observer;

use Magento\Framework\Event\Observer;
use Magento\Framework\Event\ObserverInterface;

class {OBSERVER} implements ObserverInterface
{
    /**
     * Functionality that will fire whenever the event runs
     *
     * @param Observer $observer
     */
    public function execute(Observer $observer)
    {
        // Your code goes here...

        // Example for PDP Event
        /*$product = $observer->getProduct();
        $originalName = $product->getName();
        $modifiedName = $originalName . ' - Modified by autogenerated WH Observer';
        $product->setName($modifiedName);*/

        // Get params
        // $params = $observer->getEvent();
    }
}